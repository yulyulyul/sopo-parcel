spring:
  application:
    name: parcel-service
---
spring:
  cloud:
    config:
      profile: stage
      uri: http://config:8888
      #      todo mariadb2 => mariadb
      name: default, parcel-service, tracing, kafka, eureka, mariadb2, firebase, discovery
---
spring:
  cloud:
    config:
      profile: local
      uri: http://localhost:8888
      #      todo mariadb2 => mariadb
      name: default, parcel-service, tracing, kafka, eureka, mariadb, firebase, discovery
---
spring:
  cloud:
    config:
      profile: prelive
      uri: http://config:8888
      #      todo mariadb2 => mariadb
      name: default, parcel-service, tracing, kafka, eureka, mariadb2, firebase, discovery