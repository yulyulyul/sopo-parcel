spring:
  application:
    name: parcel-service
  config:
    activate:
      on-profile: local
  cloud:
    config:
      uri: http://localhost:8888
      name: default, parcel-service, tracing, kafka, eureka, mariadb, firebase
